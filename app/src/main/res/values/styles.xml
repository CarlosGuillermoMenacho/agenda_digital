<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="MyTheme.Personalizado"></style>

    <style name="SplashThme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/backgroud_splash</item>
        <item name="android:statusBarColor">@color/color_surface</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


</resources>